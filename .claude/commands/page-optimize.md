---
description: On-page оптимизация одной страницы bixbit.io/en под целевой запрос — мета, заголовки, структура, schema, перелинковка, passage-citability.
argument-hint: "[url] [target keyword]"
---

Оптимизируй страницу **$1** под целевой запрос **$2**. Действуй по `RUNBOOK.md`/`CLAUDE.md`.

## Шаги
1. Сверь URL с `data/site-structure.md`. Если файла/записи нет — пометь и продолжай аккуратно.
2. Сними текущее состояние (мета, H1–Hn, контент, schema, внутренние ссылки) и
   реальные данные по запросу: объём/интент (DataForSEO), текущие позиции/CTR (GSC).
   Нет доступа → скажи, не выдумывай.
3. SERP-анализ интента под «$2» + проверка соответствия типа страницы (SXO).
4. Делегируй:
   - `content-seo` — title/description, H-структура, целевые сущности, FAQ, перелинковка,
     E-E-A-T. Тех-спеки прошивок → `[VERIFY]`.
   - `technical-seo` — JSON-LD под тип страницы (Product/SoftwareApplication/Article/FAQ),
     канониклы.
   - `geo-ai` — passage-структура под цитирование в AI (прямой ответ в первом абзаце,
     таблицы спеков).
5. Регрессионная проверка мета/schema **перед** предложением коммита.

## Правила
Тон — технический B2B-английский, без воды. Цифры (J/TH/%/°C/$) — только проверяемые
или `[VERIFY]`. Никаких обещаний доходности. Только белые методы.

## Артефакт
`content/page-optimize/<slug>.md` — было/стало по каждому элементу + готовые
сниппеты (мета, JSON-LD) + список `[VERIFY]`-пунктов. Правки кода — диффом для PR,
не в прод. В чат — краткое резюме изменений.
